{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"], "env": ["INSIDE_GOCLAW_GA_ID", "INSIDE_CLAUDEKIT_GA_ID"] },
    "dev": { "cache": false, "persistent": true },
    "check": { "dependsOn": ["^build"] }
  }
}
